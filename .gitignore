# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/

# Notebooks
.ipynb_checkpoints/

# Data (raw pulls / caches — keep out of git)
data/raw/
data/cache/
*.csv
*.parquet

# OS / editor
.DS_Store
.vscode/
.idea/

# Local secrets (see .env.example for what's expected)
.env

# SQLite (rebuilt by scripts/import_ipeds.py)
*.db
*.db-wal
*.db-shm
data/

# Output of the local watermark-removal tool, which writes a sibling copy
# next to whatever it inspects. Never the file we want in the repo.
*.cleaned.*
